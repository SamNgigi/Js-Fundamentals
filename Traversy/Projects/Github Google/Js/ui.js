class UI {
  constructor() {
    // Grabbing the HTML element with the id of profile.
    this.profile = document.getElementById("profile");
  }


  showProfile(user) {
    // console.log(user);
  }
}

export default UI;